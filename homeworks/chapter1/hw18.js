// Homework 16: Create toy1, color1, length1, width1, price1 variables. Print them using string cocatenation.
//  Create  toy2, color2, length2, width2, price2 variables.Print them using the template literal.

// Homework 17: Create variable laptopName, laptopSize(13.3 inches or 14.1 inches or 15.6 inches), 
// memory(4GB or 8GB or 16GB), processingCores(2 or 4 or 8 ) , price. print the data using template literals. 
// Then change the details in the same variables and print the data again with template literal.

// Homework 18: Use same variables as homework 16 and display the details in table structure using escape sequences.
// Homework 19: Use same variables as homework 17 and display the details in table structure using escape sequences.

let toy1 = "woody";
let color1 = "brown";
let length1 = 3;
let width1 = 2;
let price1 = 50;
Math.random()
let toy2 = "buzz";
let color2 = "green";
let length2 = 4;
let width2 = 3;
let price2 = 67;

//console.log(`${toy1} is ${color1} and he is ${length1}\n long and ${width1} wide and he cost ${price1}`);

console.log(`${toy1} \t ${color1} \t ${length1} \t ${width1} \t ${price1}`);
console.log(`${toy2} \t ${color2} \t ${length2} \t ${width2} \t ${price2}`);
